if (!("a" in window)) { // в if видно что window.a уже есть
    var a = 1; //поэтому строка не стработает 
}
alert(a); //ответ undefined
-------------------------------------------------------------------
var b = function a(x) {  
    x && a(--x);
};
alert(a); // переменна€ a не определена 
-------------------------------------------------------------------
function a(x) {
    return x * 2;
}
var a; // объ€вили переменную и присвоили текст функции
alert(a);
-------------------------------------------------------------------
function b(x, y, a) {
    arguments[2] = 10; //номер аргумента считаетс€ с 0,
    alert(a); // ответ 10 
}
b(1, 2, 3);
-------------------------------------------------------------------
function a() {
    alert(this);
}
a.call(null); // ответ window, т.к мы вызвали фунцкию, а call и null не вли€ют на результат 